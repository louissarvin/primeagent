<p align="center">
  <img src="web/public/assets/PrimeAgentLogo.svg" width="80" height="80" alt="PrimeAgent Logo" />
</p>

<h1 align="center">PrimeAgent</h1>

<p align="center">
  <strong>The prime brokerage layer for AI agents.</strong><br/>
  Cross-domain margin across off-chain Robinhood (MCP) and on-chain Robinhood Chain.
</p>

Submitted to the Arbitrum Open House London Buildathon 2026.

## What it is

One AI agent. One margin account. Two domains:

- **Off-chain**: Robinhood's [Agentic Trading MCP](https://robinhood.com/us/en/agentic-trading/) (launched 27 May 2026)
- **On-chain**: [Robinhood Chain L2](https://docs.robinhood.com/chain/) (Arbitrum Orbit, chain id 46630) holding tokenised TSLA, AMZN, PLTR, NFLX, AMD

Cross-domain netting cuts margin requirement by up to 100% for hedged positions. The cross-domain math runs in a Stylus Rust engine on Arbitrum, state lives behind an [ERC-2535](https://eips.ethereum.org/EIPS/eip-2535) Diamond, per-agent permissions are [ERC-7715](https://eips.ethereum.org/EIPS/eip-7715) grants on a ZeroDev Kernel v3.1 smart account, identity and reputation come from [ERC-8004](https://eips.ethereum.org/EIPS/eip-8004), and position state is bound to an NFT via [ERC-6551](https://eips.ethereum.org/EIPS/eip-6551).

## Repository layout

| Workspace | Stack | Purpose |
|---|---|---|
| `web/` | TanStack Start + React 19 + wagmi v2 + ZeroDev Kernel v3.1 | Operator dashboard, mint flow, OAuth callback, chat panel |
| `backend/` | Bun + Fastify 5 + Prisma 7 + Postgres | MCP server + client, SIWE, agent runtime, Claude chat, EIP-712 attestor |
| `contracts/` | Foundry, solc 0.8.35, OpenZeppelin v5 | PrimeAgent Diamond, AgentVault (ERC-4626), PositionNFT (ERC-721), Adapters, DEX, audit facet |
| `stylus/` | Rust + stylus-sdk 0.10.7 | margin_engine (Q96.48 cross-domain netting), risk_engine, quic_arithmetic |
| `docs/` | Markdown | Integration map, demo script, ADRs |

## Demo path (`docs/DEMO_SCRIPT.md`)

```
1. Connect wallet (no ETH needed on Arb Sepolia, paymaster sponsors)
2. SIWE -> JWT
3. /launch -> pick risk profile -> ERC-7715 grant -> Kernel mint
4. /agent/$tokenId
     AgentHeader: status, ERC-8004 reputation, LangSmith link
     CrossDomainHedge: capital saved by netting
     MarginStats: live Stylus reads
     VarBadge: 99% one-day parametric VaR
     MarginCallSimulator: drag slider, watch naive vs hedged liquidation
     PnlCard + RhChainPositionCard + PositionsTable + ActionsLog
5. Ask the agent (Claude grounded on the live snapshot)
6. Edit policy (Diamond.updatePermission)
7. Revoke (Diamond.revokePermission)
```

## Live contract addresses (Arbitrum Sepolia, chain 421614)

Generated by `contracts/script/extract-addresses.mjs`. Canonical at `contracts/addresses.json`.

| Contract | Address |
|---|---|
| PrimeAgentFactory | `0x8235890d157f7c67ed6bcd42b0c2137942b8ba38` |
| PrimeAgentDiamond | `0x56c780fcf163596b59998e737898d1055c69d69b` |
| PositionNFT | `0x98881c49d00b66febbfd3172f9de0f98df7ad1ff` |
| AgentVault impl | `0xa442d6899c38caeccee0a5a79882633b105647e0` |
| RobinhoodMcpAttestor | `0x6a31469e1aef69cec8466399d94456ad4555ad41` |
| PriceOracle | `0xb83a5ff4a33111e8b07adc843fdb2d782826dca3` |
| Stylus margin_engine | `0x43d0c3365fdf1706bd1236d14502890278bd0cd9` |
| Erc7715PolicyAuditFacet | `0x3709eaca94d3aecca22d3909c4fd8d6a94bcd198` |
| RhChainSwap (chain 46630) | `0xc346333ea7Dc98FDDF752FdBd5928CE2460a8C7B` |

The Stylus margin_engine is initialised with per-asset margin params:
TSLA mr=3000/lt=2500, AMZN/PLTR/NFLX/AMD mr=2500/lt=2000.

## Quick start

```bash
# Backend
cd backend
cp .env.example .env             # fill JWT_SECRET, ATTESTOR keys, ANTHROPIC_API_KEY
bun install
bun db:push                       # run yourself; never destructive
bun dev                           # 3700

# Web (separate terminal)
cd web
# .env needs VITE_PUBLIC_BACKEND_URL, VITE_ZERODEV_PROJECT_ID
bun install
bun dev                           # 3200

# Open http://localhost:3200
```

Backend `/health` returns `{ ready: true }` when DB, attestor parity, Claude key, and on-chain wiring are all green. Use that as the single signal.

## Production posture

| Concern | How |
|---|---|
| CORS | `PUBLIC_ORIGIN`-driven allowlist; permissive in dev |
| CSP | `web/vercel.json` headers (default-src self, strict connect-src) |
| HSTS | `Strict-Transport-Security: max-age=63072000; preload` |
| Attestor parity | Boot-time guard fails the process on mismatch in prod (`backend/src/lib/attestorBoot.ts`) |
| Token rotation | OAuth refresh 5 min before expiry (`tokenRefresher` worker) |
| Replay protection | EIP-712 `nullifier` unique-indexed at DB + contract |
| Chat cost | Hard 10/min/user rate limit on `/api/agent/:tokenId/ask` |

## Documentation

| File | What |
|---|---|
| `PrimeAgent.md` | The 2858-line spec. Source of truth. Read first. |
| `docs/INTEGRATION.md` | Architecture map + data flow + wave roadmap status |
| `docs/DEMO_SCRIPT.md` | Three-minute recordable demo script |
| `docs/ADR-005-risk-profiles.md` | Why profiles drive both caps and strategy |
| `docs/ADR-006-chat-grounded-on-context.md` | Why the chat panel never free-forms |
| `docs/ADR-007-aa-and-7715-phase-3a.md` | ZeroDev Kernel + ERC-7715 phasing |

## Features (June 2026)

Wave delivery of Features A through I (see [`CHANGELOG.md`](./CHANGELOG.md) and [`docs/architecture.md`](./docs/architecture.md)).

| # | Capability | Status |
|---|---|---|
| A | Conversational policy and risk profile builder (tool-strict Claude → `AgentPolicyDraft` → ERC-7715 grant) | shipped |
| B | Live policy edit with diff + dual-sign atomic rotation (`executeBatch([revoke, install])`) | shipped |
| C | Five risk profile templates with on-chain `presetHash` committed via `LibRiskPresets` | shipped |
| D | Bot-builds-bot fleet spawn via chat (`agent.spawn` MCP tool + `POST /api/agent/fleet/spawn`) | shipped |
| E | Stylus stateless `markToMarketBasket` co-processor and `useBasketMarkToMarket` vault flag | shipped |
| F | Risk engine activation with `var/onchain` endpoint and parametric off-chain fallback | shipped |
| G | ERC-8004 reputation pill loop (cron worker writes hourly feedback per agent) | shipped |
| H | Liquidation drill button (Arb Sepolia only, 60s DB-backed cooldown, SSE lifecycle) | shipped |
| I | Hardening (CSP/HSTS/X-Frame-Options, alpine multi-stage Dockerfile, TanStack Query migration) | shipped |

## License

See `LICENSE`.
